The V4 backend must answer GET /health with status=ok before the frontend is connected. After deployment, set MARKET_API_BASE_URL to the service HTTPS URL and MARKET_AI_REALTIME_WS_URL to the same service with wss:// and /stream.