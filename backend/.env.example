# Server-side only. Never commit real credentials.
TWELVE_DATA_API_KEY=replace_with_provider_key
# Optional production frontend origin, e.g. https://your-site.example
ALLOWED_ORIGIN=https://your-site.example
