# Live deployment status

The repository is **live-backend ready**, but the actual external Netlify site and provider credentials must be configured in the owner's Netlify account.

No API keys are stored in this repository.

After Netlify is configured, set the public Functions URL in `src/config.js` and redeploy GitHub Pages.
