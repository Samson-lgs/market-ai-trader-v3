# Live backend readiness

Implemented in the repository:

- runtime public backend configuration
- Netlify health endpoint
- hardened market-data CORS and validation
- hardened calendar CORS and validation
- manual GitHub Actions Netlify deployment workflow
- live deployment and end-to-end QA checklists
- server-side credential guidance

Remaining account-level action: create/configure the Netlify site and add provider secrets. The repository intentionally cannot store those secrets.
